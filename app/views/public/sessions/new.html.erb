<div class='container'>
  <div class='row'>
    <div class="mx-auto col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0">
      <h2>ログイン</h2>
      <p>会員の方はこちらからログイン</p>
      <%= form_with model: [:public,@user], url: user_session_path, id: 'user', class: 'user', local: true do |f| %>
      <%= alert %>
      <div class="field">
        <%= f.label :name,'ユーザー名' %><br />
        <%= f.text_field :name, autofocus: true, autocomplete: "email", class: 'form-control' %>
      </div>
      <div class="field">
        <%= f.label :password,'パスワード' %><br />
        <%= f.password_field :password, autocomplete: "current-password", class: 'form-control' %>
      </div>
      <div class="actions">
        <%= f.submit "ログイン", class: 'btn btn-primary' %>
      </div>
      <% end %>
      <p>会員登録がお済みでない方
        <br><%= link_to "こちら", new_user_registration_path %> からログインしてください。
      </p>
    </div>
  </div>
</div>
